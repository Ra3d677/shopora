"use client";
export default function TwoHAboutUsManager({ slug, initialSettings }: { slug: string; initialSettings: any }) {
  return <div className="max-w-4xl mx-auto p-8 text-center text-slate-400 text-sm">2H Theme about us manager is not yet available.</div>;
}
