"use client";
export default function TwoHTemplate(props: any) {
  const { store, banners, settings, products, slug, categories } = props;
  return <div className="p-8 text-center text-slate-400">2H Theme template is not yet available.</div>;
}
